public with sharing class ONE_HeaderData extends ONE_PdfServiceBase.Header {
    public ONE_HeaderData(ONE_PdfServiceBase.Block leftBlock, ONE_PdfServiceBase.Block rightBlock, ONE_PdfServiceBase.Block centralBlock) {
        super(leftBlock, rightBlock, centralBlock);
    }
}